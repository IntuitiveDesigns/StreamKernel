# =============================================================================
# BENCHMARK SECURE (Prometheus)
# =============================================================================
pipeline.name=StreamKernel-Bench
pipeline.version=bench

metrics.type=PROMETHEUS
metrics.prometheus.port=8080

# Security on, durability relaxed
kafka.producer.acks=1
kafka.producer.idempotence=false
