# =============================================================================
# METRICS BASE (Vendor-agnostic)
# =============================================================================
metrics.enabled=true
metrics.step.seconds=10

metrics.service=streamkernel
metrics.env=local

# Optional global tags
metrics.tags.team=platform
metrics.tags.owner=streamkernel
